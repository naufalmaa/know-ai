model_list:
  - model_name: deepseek-r1:14b
    litellm_params:
      model: ollama/deepseek-r1:14b
      api_base: ${OLLAMA_BASE}
      # api_key: ${LITELLM_API_KEY}
  - model_name: mxbai-embed-large d2
    litellm_params:
      model: ollama/mxbai-embed-large:latest
      api_base: ${OLLAMA_BASE}
      # api_key: ${LITELLM_API_KEY}

# general_settings:
#   master_key: ${LITELLM_API_KEY}

server_settings:
  host: 0.0.0.0
  port: 4000
