model_list:
  - model_name: deepseek-r1:14b
    litellm_params:
      model: ollama/deepseek-r1:14b
      api_base: http://117.54.250.177:5162
      # api_key: ${LITELLM_API_KEY}
  - model_name: mxbai-embed-large:latest
    litellm_params:
      model: ollama/mxbai-embed-large:latest
      api_base: http://117.54.250.177:5162
      # api_key: ${LITELLM_API_KEY}

general_settings:
  master_key: sk-local
  # Disable database - this prevents Prisma errors
  use_azure_key_vault: false
  disable_spend_logs: true
  disable_master_key_return: true
  # Enable fallback to OpenAI if needed
  fallback_models: ["openai/gpt-5-nano"]
  # allow requests even if DB is unavailable
  allow_requests_on_db_unavailable: true
  # Disable database completely
  database_url: null  

server_settings:
  host: 0.0.0.0
  port: 4001